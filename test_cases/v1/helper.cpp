int global_add(int x, int y) { return x + y; }

// Function with clean formatting
int calculate_score(int points, int bonus) {
  if (points > 100) {
    return points + bonus;
  }
  return points;
}

void will_be_deleted() {
  // bye
}
