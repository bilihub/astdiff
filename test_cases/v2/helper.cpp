int global_add(int x, int y) {
  // Added logging
  return x + y;
}

// Function with messy formatting (only spacing/newlines changed)
int calculate_score(int points, int bonus) {
  if (points > 100) {

    return points + bonus;
  }
  return points;
}

void new_global_function() {
  // new func
}
